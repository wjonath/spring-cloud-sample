server:
  port: 6002

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6006/eureka/

spring:
  application:
    name: micro-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
#  cloud:
#    config:
#      uri: http://localhost:6001
