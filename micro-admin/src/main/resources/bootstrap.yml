spring:
  application:
    name: micro-admin
  boot:
    admin:
#      routes:
#        endpoints: env,metrics,trace,dump,jolokia,info,configprops,trace,logfile,refresh,flyway,liquibase,heapdump,hystrix.stream
      context-path: /sba

eureka:
  client:
    service-url:
      defaultZone: http://eureka.iqoo.cn/eureka/

# 测试,不使用密码
server:
  port: 6005

management:
  security:
    enabled: false
