eureka:
  client:
    service-url:
      defaultZone: http://eureka.iqoo.cn/eureka/
spring:
  cloud:
    config:
#      profile: @package.environment@
      discovery:
        enabled: true
        service-id: micro-config