eureka:
  client:
    service-url:
      defaultZone: http://www.iqoo.cn:6006/eureka/
spring:
  cloud:
    config:
#      profile: @package.environment@
      discovery:
        enabled: true
        service-id: micro-config